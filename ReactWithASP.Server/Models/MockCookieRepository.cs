namespace ReactWithASP.Server.Models
{
    //public class MockCookieRepository : ICookieRepository
    //{


    //    public IEnumerable<Cookie> AllCookies =>

    //          Enumerable.Range(1, 5).Select(index => new Cookie
    //          {
    //              Date = DateOnly.FromDateTime(DateTime.Now.AddDays(index)),
    //              Id = index,
    //              name = Tools.Summaries[Random.Shared.Next(Tools.Summaries.Length)],
    //              desc = "sweet",
    //              Price = 1.99
    //          })
    //       .ToArray();

    //    public Cookie? GetCookieById(int id) => AllCookies.FirstOrDefault(c => c.Id == id);
    //}
}
